<table class="table table-bordered table-striped mb-none" id="accountsummarytb">
    <thead>
        <tr>
            <th>Account Number</th>
            <th>Account Name (@ViewBag.AccountType)</th>
            @foreach (string cur in Enum.GetNames(typeof(UsedCurrencies)))
            {
                <th>@cur</th>
            }
            @*<th>Balance(@ViewBag.AccountType)</th>
            <th>Currency</th>*@
        </tr>
    </thead>
    <tfoot>
        <tr>
            <th> </th>
            <th> </th>
            @foreach (string cur in Enum.GetNames(typeof(UsedCurrencies)))
            {
                <th></th>
            }
    </tfoot>
</table>